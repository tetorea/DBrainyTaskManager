module Ressources;

// liste des elements pouvant etre utilises par les actions mais une seule action peut utiliser la même ressource a la fois!
enum SystemRessources { NULL, CAMERA, WHEEL, NECK, MIC, SPEAKER, ARM, SCREEN }

static const string State_AtHome = "At Home";
